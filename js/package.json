{
  "name": "@flarum/flarum-ext-emoji",
  "version": "0.0.0",
  "dependencies": {
    "emojibase-data": "^3.0.0",
    "flarum-webpack-config": "0.1.0-beta.9",
    "textarea-caret": "^3.1.0",
    "webpack": "^4.19.1",
    "webpack-cli": "^3.1.0",
    "twemoji": "^11.0.0"
  },
  "scripts": {
    "build": "npm run generate-emoji-map; webpack --mode production",
    "watch": "npm run generate-emoji-map; webpack --mode development --watch",
    "generate-emoji-map": "node ./scripts/generate-emoji-map.js"
  }
}
